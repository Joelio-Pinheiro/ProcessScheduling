# Simulador de Escalonamento de Processos

## Como Rodar

1. Configurar o arquivo de configuração e o de processos como quiser

2. No terminal, dentro da pasta do projeto, basta rodar:

```bash
python main.py < processos.txt